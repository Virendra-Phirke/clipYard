{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": "auth != null && auth.token.roomId === $roomId",
        ".write": false,
        "meta": {
          ".read": "auth != null && auth.token.roomId === $roomId"
        },
        "presence": {
          ".read": "auth != null && auth.token.roomId === $roomId",
          "$uid": {
            ".write": "auth != null && auth.token.roomId === $roomId && auth.uid === $uid"
          }
        },
        "clip": {
          ".read": "auth != null && auth.token.roomId === $roomId",
          "updatedAt": {
            ".read": "auth != null && auth.token.roomId === $roomId"
          }
        }
      }
    }
  }
}
